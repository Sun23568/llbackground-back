<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.llback.dal.ai.dao.ChatHistoryMapper">

    <insert id="insert" parameterType="com.llback.dal.ai.po.ChatHistoryPo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_history (
            conversation_id,
            user_id,
            model_id,
            menu_code,
            user_message,
            ai_response,
            create_time
        ) VALUES (
            #{conversationId},
            #{userId},
            #{modelId},
            #{menuCode},
            #{userMessage},
            #{aiResponse},
            #{createTime}
        )
    </insert>

</mapper>
