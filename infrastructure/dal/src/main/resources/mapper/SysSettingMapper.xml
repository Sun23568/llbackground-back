<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.llback.dal.setting.dao.SysSettingDao">

    <!-- 查询所有配置，返回 key->value 的 List -->
    <select id="queryAll" resultType="com.llback.dal.setting.po.SysSettingPo">
        SELECT cfg_key, cfg_value
        FROM sys_setting
        ORDER BY cfg_key
    </select>

    <!-- 查询单个配置 -->
    <select id="queryByKey" resultType="com.llback.dal.setting.po.SysSettingPo">
        SELECT cfg_key, cfg_value
        FROM sys_setting
        WHERE cfg_key = #{cfgKey}
    </select>

    <!-- 插入或更新 -->
    <insert id="saveOrUpdate">
        INSERT INTO sys_setting (pk_id, cfg_key, cfg_value, update_time)
        VALUES (#{pkId}, #{cfgKey}, #{cfgValue}, NOW())
        ON DUPLICATE KEY UPDATE
            cfg_value   = #{cfgValue},
            update_time = NOW()
    </insert>

</mapper>
