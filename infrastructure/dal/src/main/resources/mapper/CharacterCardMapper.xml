<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.llback.dal.ai.dao.CharacterCardDao">
    <!-- 插入角色卡 -->
    <insert id="insert" parameterType="com.llback.dal.ai.po.CharacterCardPo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO character_card (
            id, user_id, card_name, user_name, character_name, card_description, card_content,
            initial_prompt, first_mes, avatar, mes_example, scenario,
            delete_status, create_time, update_time
        ) VALUES (
            #{id}, #{userId}, #{cardName}, #{userName}, #{characterName}, #{cardDescription}, #{cardContent},
            #{initialPrompt}, #{firstMes}, #{avatar}, #{mesExample}, #{scenario},
            1, NOW(), NOW()
        )
    </insert>

    <!-- 根据ID查询角色卡 -->
    <select id="selectById" resultType="com.llback.dal.ai.po.CharacterCardPo">
        SELECT
            id,
            user_id,
            card_name,
            user_name,
            character_name,
            card_description,
            card_content,
            initial_prompt,
            first_mes,
            avatar,
            mes_example,
            scenario,
            delete_status,
            create_time,
            update_time
        FROM character_card
        WHERE id = #{id}
          AND delete_status = 1
    </select>

    <!-- 根据用户ID查询角色卡列表 -->
    <select id="selectByUserId" resultType="com.llback.dal.ai.po.CharacterCardPo">
        SELECT
            id,
            user_id,
            card_name,
            user_name,
            character_name,
            card_description,
            initial_prompt,
            avatar,
            scenario,
            delete_status,
            create_time,
            update_time
        FROM character_card
        WHERE user_id = #{userId}
          AND delete_status = 1
        ORDER BY create_time DESC
    </select>

    <!-- 更新角色卡 -->
    <update id="update" parameterType="com.llback.dal.ai.po.CharacterCardPo">
        UPDATE character_card
        SET card_name = #{cardName},
            user_name = #{userName},
            character_name = #{characterName},
            initial_prompt = #{initialPrompt},
            update_time = NOW()
        WHERE id = #{id}
          AND delete_status = 1
    </update>

    <!-- 逻辑删除角色卡 -->
    <update id="deleteById">
        UPDATE character_card
        SET delete_status = 2,
            update_time = NOW()
        WHERE id = #{id}
    </update>
</mapper>
